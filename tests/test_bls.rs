//use miracl_core::bls12381::bls::{BLS_OK}; 
//use dvf::crypto::bls::{Bls};
//use dvf::crypto::define::{MB};


//#[test]
//fn test_bls() {
    //let mut m_bls = Bls::new();
    //m_bls.keygen();

    //println!("{:02x?}", m_bls.sk); 

    //let message = "hello world";
    //let mut sig: [u8; MB+1] = [0; MB+1];
    //m_bls.sign(&mut sig, message.as_bytes());

    //let status = m_bls.verify(&sig, message.as_bytes());
    
    //assert_eq!(status, BLS_OK);
//}

